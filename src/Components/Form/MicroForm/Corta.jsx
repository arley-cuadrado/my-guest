import React from "react"

const Corta = (props) => {

        console.log(props)

        return(
            <div>
                Respuesta corta, ID:  {props.id} 
            </div>
        )
    }


export default Corta