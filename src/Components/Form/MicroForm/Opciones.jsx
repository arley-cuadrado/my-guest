import React from "react"

const Opciones = (props) => {

        console.log(props)

        return(
            <div>
                Varias opciones, ID:  {props.id} 
            </div>
        )


}

export default Opciones